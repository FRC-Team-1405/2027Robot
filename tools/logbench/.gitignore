web/node_modules/
web/dist/
web/tsconfig.tsbuildinfo
# Dev fixture: a real log's spec, ~2MB, regenerate with
#   python server/dump_spec.py <some.wpilog> --out web/public/sample-spec.json
web/public/sample-spec.json
__pycache__/
# Per-machine remote host/user/path config (bench vs. competition vs. home differ) --
# see server/remote_config.json.example for the committed template.
server/remote_config.json

# Per-server-run diagnostic logs. Keep the directory and its .gitkeep in the repo.
logs/*
!logs/.gitkeep

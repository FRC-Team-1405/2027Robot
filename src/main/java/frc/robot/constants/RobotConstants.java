package frc.robot.constants;

import edu.wpi.first.math.util.Units;

public class RobotConstants {
    // Robot dimensions including bumpers
    public static final double ROBOT_WIDTH = Units.inchesToMeters(33);
    public static final double HALF_ROBOT_WIDTH = ROBOT_WIDTH / 2.0;

    // length the intake extends past the bumper
    public static final double INTAKE_EXTENSION_LENGTH = Units.inchesToMeters(9);
}
